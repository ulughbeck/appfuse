name: appfuse
description: DI + Config + Locale + Theme + Permissions

version: 0.0.1

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl:
  shared_preferences: ^2.5.3
  package_info_plus: ^8.3.1
  platform_info: ^5.0.0
  permission_handler: ^12.0.1
  http: ^1.5.0

dev_dependencies: 
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
